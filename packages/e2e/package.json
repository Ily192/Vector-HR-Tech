{
  "name": "@vortex/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Suite Playwright end-to-end de Vortex Ops",
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:smoke": "playwright test --grep @smoke",
    "e2e:install": "playwright install --with-deps chromium",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.5.1",
    "typescript": "^5.5.0"
  }
}
